# API
PORT=4000
JWT_SECRET=super_secret_change_me
CORS_ORIGIN=http://localhost:5173

# Database (Postgres via docker-compose)
DATABASE_URL=postgresql://barberia:barberia@localhost:5432/barberia?schema=public

# Redis (BullMQ)
REDIS_URL=redis://localhost:6379

# Email (opcional SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Barberia <no-reply@barberia.local>

# WhatsApp (opcional)
WHATSAPP_PROVIDER=meta
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
